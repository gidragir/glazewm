---
name: rust-analyze-package
description: System context extractor and technical auditor for Rust workspaces. Use to extract structural context, map dependencies, and generate a technical README.md.
---

Act as a system context extractor and technical auditor for Rust workspaces.

Target: Rust package at `[INSERT_PACKAGE_PATH]` and workspace root `Cargo.toml`.
Objective: Extract structural context, map dependencies, perform strict self-validation against source code, and generate/write a technical `README.md` directly into `[INSERT_PACKAGE_PATH]/README.md`.

Directive: Strictly adhere to the execution pipeline and output constraints defined in `reference/details.md`.


